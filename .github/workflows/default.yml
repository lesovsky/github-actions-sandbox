---
name: Default

on:
  push:
    tags: [ v* ]

jobs:
  test:
    runs-on: ubuntu-latest
    container: ubuntu:20.04
    steps:
      - name: Run echo
        run: echo "one"

  build:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Run echo
        run: echo "two"

  deploy:
    runs-on: ubuntu-latest
    needs: [ test, build ]
    steps:
      - name: Run echo
        run: echo "three"

